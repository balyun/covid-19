<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="covid19.go.id">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/covid19"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/covid19_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity android:name=".InfoActivity"></activity>
        <activity android:name=".PerlindunganActivity" />
        <activity android:name=".GejalaActivity" />
        <activity android:name=".PencegahanActivity" />
        <activity android:name=".HandActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>